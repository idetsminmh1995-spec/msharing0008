# src/core

The data model — instrument-agnostic. No SVG, no rendering, no math beyond
what's needed to describe a score: `Pitch`, `Duration`, `Note`, `Rest`,
`Chord`, `Voice`, `Measure`, `Part`, `Score`.

See PLAN.md §6 and §4.3 (the "any instrument, any line count" architectural
requirement) and Phase 4 (duration/tick math).

Nothing here yet -- Phase 1 is folder structure only.
