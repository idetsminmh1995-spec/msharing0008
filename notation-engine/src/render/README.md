# src/render

Builds the actual SVG DOM/markup from the geometry computed in
src/geometry. This is the only layer that touches SVG elements directly.

See PLAN.md Phase 6 (SVG primitives layer) and every drawing-facing part of
Groups B-D.

Nothing here yet -- Phase 1 is folder structure only.
