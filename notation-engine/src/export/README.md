# src/export

SVG -> PNG rasterization, direct SVG export, and multi-page PDF export.

See PLAN.md §3 (scope) and §16.2 (page layout, which PDF export uses).

Nothing here yet -- Phase 1 is folder structure only.
