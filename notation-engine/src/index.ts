// Notation Engine -- entry point.
//
// This file is currently just a toolchain smoke-test stub for Phase 2
// (build/typecheck/lint/format all need at least one real .ts file to run
// against). Real exports start arriving in Phase 3 (core data model).

/** Semantic version of the engine itself, bumped by hand for now. */
export const ENGINE_VERSION = '0.0.0';
