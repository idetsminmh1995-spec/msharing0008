# src/layout

The two layout modes -- horizontal continuous-scroll and paginated -- plus
the formatter/spacing algorithm and responsive W x H resize.

See PLAN.md Group G (Phase 41-44), especially Phase 44 (arbitrary
width/height resize, a hard requirement).

Nothing here yet -- Phase 1 is folder structure only.
